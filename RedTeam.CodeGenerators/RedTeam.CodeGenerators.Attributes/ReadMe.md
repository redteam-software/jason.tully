# Osh.KafkaFlow.CodeGenerators.Attributes

Defines attributes that can be used to auto-generate KafkaFlow consumer and producer registrations.

The names of the attributes mirror the `Add` registration methods defined in `ConsumerMiddlewareBuilderExtensions` and `KafkaClientBuilder`